[package]
name = "previews"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow = "1"
serde_json = "1"
symphonia = { version = "0.5", features = ["mp3", "aac", "isomp4", "aiff"] }
walkdir = "2"
